interface:
  display_name: "Orchestrate Workers"
  short_description: "Coordinate CCM worker rooms"
  default_prompt: "Use $orchestrate-workers to split this CCM stage across visible worker rooms, collect Worker Reports, and archive only after output is consumed."
