interface:
  display_name: "Orchestrate Workers"
  short_description: "Coordinate CCM worker rooms"
  default_prompt: "Use $orchestrate-workers to coordinate this CCM stage across visible worker rooms."
