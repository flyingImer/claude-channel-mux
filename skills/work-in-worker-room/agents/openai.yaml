interface:
  display_name: "Work In Worker Room"
  short_description: "Complete a CCM worker task"
  default_prompt: "Use $work-in-worker-room to complete this assigned CCM worker task, verify the result, and produce a Worker Report."
