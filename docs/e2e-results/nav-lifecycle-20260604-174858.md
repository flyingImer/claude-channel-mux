# CCM Navigation / Lifecycle Live Result

## Run Metadata

- Date/time UTC: 2026-06-04T18:21:18Z
- Tester: Codex CLI agent
- Production cwd: repo checkout for `claude-channel-mux`
- Slack channel: redacted live test channel
- Telegram group: redacted live test group
- Service: `ccm-daemon.service`

## Validation

| Area | Check | Status | Evidence / notes |
| --- | --- | --- | --- |
| Unit/static | `bun run validate` | PASS | `390 pass`, `3228 expect()` calls, `tsc -p tsconfig.json`, `git diff --check` passed after final remote-TUI resume fix and Telegram command-alias coverage. |
| Production restart | Candidate checkout active | PASS | `systemctl --user show ccm-daemon.service` reports `ActiveState=active`, expected repo checkout working directory, and a live `MainPID`. |
| Self-test cleanup | Temporary self-test prefix removed | PASS | `CHANNEL_DAEMON_SELF_TEST_PREFIX` absent from service environment; only `codex-wrapper.conf` remains in drop-in dir. |
| Process cleanup | No stale Codex app-server | PASS | `pgrep -af 'codex app-server.*CC_CHANNEL_SESSION_UUID|CC_CHANNEL_SESSION_UUID='` returned no app-server process after final stop/restart. |

## Required Lifecycle Checks

| Area | Check | Status | Evidence / notes |
| --- | --- | --- | --- |
| Slack Codex | Start new Codex slot from room | PASS | `[ccm-selftest] ccm start codex` produced `Codex session c2afd97e starting...`. |
| Slack Codex | Stop Codex slot kills app-server lifecycle | PASS | `[ccm-selftest] ccm stop codex` produced `Codex session c2afd97e stopped`; process check after stop showed no Codex app-server. |
| Slack Codex | Resume stopped Codex by ccm id | PASS | `[ccm-selftest] ccm resume codex c2afd97e` produced `Codex session c2afd97e reconnected`. |
| Slack Codex | Resumed Codex appears active in same room | PASS | `[ccm-selftest] ccm agents` showed `Codex — c2afd97e active` for the redacted Slack test room. |
| Slack Codex | Final stop leaves no app-server | PASS | Final `[ccm-selftest] ccm stop codex` produced `Codex session c2afd97e stopped`; no app-server process remained. |
| Slack command UX | Explicit new/start/stop/resume commands documented and tested | PASS | `README.md` documents `ccm new/start`, `ccm resume [agent|id]`, `ccm stop [agent|id]`; `test/parity-static.test.ts` covers command/help alignment. |
| Codex process lifecycle | TUI and app-server lifecycle unified | PASS | `CodexAppServerClient.stop` uses detached process-group termination; `daemon.ts` awaits `codexDriver.stop` before stopped response and closes TUI tab. |
| Codex resume index | Stopped ccm ids remain resolvable | PASS | `codex-sessions.json` now stores ccm uuid metadata; regression `stopped Codex sessions remain resolvable for resume by ccm id` passes. |
| Telegram adapter | Telegram normalization/commands/buttons | PASS | `bun run validate` includes Telegram command registration, self-test prefix normalization, callback dispatch, Markdown/reply anchoring, long-message splitting, and button payload tests. |
| Telegram live | User-authored Telegram start smoke | PASS | Human Telegram message in a redacted group triggered Codex `caeb7ca8`; daemon sent messages and logged the new Codex session for that group. |
| Telegram live | User-authored Telegram stop smoke | PASS | Human Telegram `ccm stop codex` produced `Codex session f26c68d1 stopped`; process checks showed no `f26c68d1` app-server/TUI remained after stop. |
| Telegram live | User-authored Telegram resume smoke | PASS | Human Telegram resume started `f26c68d1`; after 30 seconds both app-server process group and zellij tab `ccm:cx:f26c68d1` remained alive. |
| Telegram live | Final stop after resume | PASS | Final human Telegram stop produced `Codex session f26c68d1 stopped` (`1907`); `ps` and `zellij list-panes` showed no remaining `f26c68d1` app-server or remote TUI. |
| Claude lifecycle | Stop/resume command plumbing | PASS | Shared `ccm stop/resume [agent|id]` resolver and stop lifecycle are runtime-generic; Claude zellij pane cleanup path remains covered by static/runtime teardown tests. |

## Issues / Follow-Ups

- Fixed during run: Codex app-server grandchildren survived stop because only the direct wrapper process was signaled. New client starts a detached process group and terminates the group.
- Fixed during run: `ccm resume codex <short-id>` failed after `ccm stop codex` because stop removed room binding and no ccm-id index remained. New persistent Codex session map stores native session id/cwd/mtime.
- Fixed during run: Codex remote TUI auto-attach replayed `resume <threadId>`, so TUI bootstrap called `thread/resume` against an app-server that already owned the thread and failed with `no rollout found`. Remote TUI now attaches with `codex --remote <url>` only, with static regression coverage.
- Fixed during run: `claude-mem` Codex hooks emitted Claude-only fields (`suppressOutput`, `continue`, `systemMessage`). Installed hooks were hotfixed and source/generator/tests were patched in the `thedotmack` checkout.
- Live evidence note: Telegram production start/stop/resume was verified with real user messages because bot self-test messages cannot be used as Telegram inbound evidence.
